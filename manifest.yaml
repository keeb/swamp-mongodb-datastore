manifestVersion: 1
name: "@keeb/mongodb-datastore"
version: "2026.05.05.1"
description: "MongoDB-backed swamp datastore — distributed locks with TTL, heartbeat, and nonce fencing, plus a manifest+content-addressed-blob sync protocol that streams the datastore tier as inline-bytes cursor batches. Scoped by tenant + repo namespace. Requires MongoDB 4.0+ as a replica set."
repository: https://github.com/keeb/swamp-mongodb-datastore
labels:
  - mongodb
  - datastore
  - distributed-lock
  - content-addressed
  - replica-set
platforms: []
additionalFiles:
  - README.md
  - LICENSE.txt
datastores:
  - mongodb/mod.ts
